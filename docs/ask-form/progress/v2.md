---
summary: Progress tracker for `ask-form` v2 — rich content (plugins/productivity). Client-side
  richness ported from rich-document into ask-form's static renderer — mermaid and highlighted,
  copyable code in any markdown, GFM alerts, diagram zoom, option `detail` with a Compare view,
  tabbed/collapsed/diff context, quote-into-note, split layout — plus context kept in the saved record.
  Milestones with SHAs; Deferred (glossary); Rejected (option↔diagram highlight); Confirmed contracts.
read_when:
  - resuming or continuing the ask-form rich-content build
  - changing static/rich.js, the markdown pipeline, vendored mermaid/highlight.js, or render_record's context output
  - deciding whether glossary popovers or option↔diagram highlighting are in scope
---

# ask-form v2 — rich content

Parent: [`v1.md`](v1.md). Design: [`../design.md`](../design.md) § *Rich content (v2)*.
Claude plan `elegant-purring-seahorse` (2026-09-23).

Origin: forms carry agent explanation, but the v1 renderer is thin — a mermaid fence in markdown
showed as code, code had no highlighting or copy, diagrams froze their theme, options could not carry
rich detail, and the record dropped every context pane. The proposal was reviewed through ask-form
itself on 2026-09-23 (record `asks/2026-09-23_0035_rich-content-for-ask-form-proposal.md`).

## Decisions (resolved 💡)

- GFM dialect (alerts, `<details>`, mermaid/diff fences), not Quarto `:::` divs: marked parses it and
  the saved record renders on GitHub and Obsidian.
- **Copy** rich-document's viewer behaviour (enlarge dialog, serial render queue, per-diagram error)
  into `static/rich.js`; no shared module while rich-document is experimental.
- **Vendor** pinned `mermaid@11.6.0` and `highlight.js@11.11.1`; CSP drops cdnjs; fully offline.
- `spec_version` stays 1: every addition is optional; answer shapes unchanged.
- Own hljs token palette on the glass tokens (no vendored theme CSS).
- Quote target: the card containing the selection, else the next question, else Comments.
- Split layout is sticky per segment (context run + the questions it precedes), not a global rail.

## Milestones

- [x] M0 — this tracker; design amendment — `0af50c3`
- [x] M1 — Tier 1 renderer: mermaid fences anywhere, code label/copy/highlight, GFM alerts,
      `<details>`, diagram enlarge, theme-aware re-render, vendored libs, CSP self-only — `74fe4ba`.
      Found on the way: v1's DOMPurify pass over Mermaid SVG blanked every `foreignObject` label;
      strict-mode output is now used as-is (rich-document precedent).
- [x] M2 — record keeps context panes (markdown, mermaid/diff fences, tab headings, image refs) — `e4665f4`
- [x] M3 — `options[].detail` + Compare view; `context.format: tabs`; `context.collapsed` — `1c019ed`
- [x] M4 — Quote into note — `b0f63df`
- [x] M5 — `context.format: diff`, ```` ```diff ```` fences, `review.items[].detail` — `260b38e`
- [x] M6 — `spec.layout: split` — `21141d8`
- [x] M7 — SKILL.md, `--example`, `--schema`, version bump; full browser pass. Found on the way: a
      fence inside `detail` broke naive parsing of the record's raw JSON tail (now a lengthened
      fence), and a quoted note rendered its `>` inline (now its own block).
- [x] KS pass in a real browser (Safari): all 11 features *Works*; Copy pasted the exact string;
      ⌘⏎ submitted; Quote landed in a note — 2026-09-23 (record
      `asks/2026-09-23_0129_ask-form-v2-real-browser-check.md`). Surfaced: split turns on by itself
      when the reader zooms out; blank right column beside a long context; footer narrower than split.
- [x] Landed on `main` via PR #64 — `938d2f6`

### v2.1 — reader settings (plan `elegant-purring-seahorse`, 2026-09-23)

- [x] Docs: this tracker; design § *Reader settings* — `be959e4`
- [x] `static/prefs.js`: cookie-backed layout + theme, `data-theme` / `data-layout` on `<html>`;
      dark tokens keyed on `data-theme`; diagrams follow the effective theme — `de499bf`
- [x] ⚙ Settings popover (Layout, Theme); `spec.layout` demoted to a hint; both split columns
      sticky; footer widens under split — `6279789`. Found on the way: picking a theme equal to the
      current effective one fired no event, leaving the radios stale (`chosen` flag added).
- [x] SKILL.md and schema description (layout is a hint); pane browser pass
- [x] KS pass in Safari: zoom-out stays one column, hint dot, Split, sticky columns, theme + diagram,
      Esc and outside click — all *Works* (record `asks/2026-09-23_0145_ask-form-v2-1-reader-settings.md`).
      KS asked for a side choice with questions left by default.
- [x] *Explanation on* Right (default) / Left under Split: `askform_side` cookie, `data-side`, CSS
      order flip only (DOM keeps explanation first); pane: right → explanation x 621 / questions x 20,
      left swaps them, the side row hides in One column
- [x] `87ebb66` — side choice. KS UAT in Safari: a second form on a new port opened with the layout
      and theme left selected; Right / Left works, Right confirmed as default (record
      `asks/2026-09-23_0148_ask-form-v2-1-remembered-settings.md`)
- [ ] Landed on `main` via PR

Evidence (pane, 2026-09-23): new port restored `askform_layout=split` / `askform_theme=system`
from the previous run; `layout: "split"` spec opens in one column with the ⚙ dot and "Suggested for
this form"; Split → grid, footer 1280 px; shorter column pins at 20 px both ways (context 778 px vs
questions 235 px, and 297 px vs 2451 px); Light under dark emulation swaps tokens and re-renders
diagrams, System follows emulation again; a layout change does not re-render diagrams; outside
click closes the popover (the pane's key injection cannot test Escape); no console errors;
`uv run tests/test_ask_form.py` OK.

## Evidence (2026-09-23)

- Dry gates: `uv run tests/test_ask_form.py` 29 tests OK (1 pty skip); `just test` all 21 suites
  exit 0; `uv run bin/validate.py` 45 skills.
- Browser (built-in pane, Chromium): mermaid in markdown, context, hidden tab and folded pane all
  rendered with labels; a broken diagram shows its error and source; Enlarge zoom (+ → 125%), close
  restores the SVG and focus; switching dark → light re-renders; Copy falls back to selecting the
  block when the clipboard is refused; GFM alerts, `<details>`, diff line classes, `<script>` in a
  diff and in markdown inert; Compare tabs sync to the chosen option, ←/→ move, two detailed
  options sit in columns at 1280 px; Quote by real mouse drag lands in the right note; split layout
  pins the context (top 330 → 20 px on scroll) at 1280 px and stacks with no horizontal scroll at
  375 px; network log shows loopback only; `--example` submitted through the UI → quoted note,
  `diverged`, and a record with context, compared/item detail, diff and a lengthened raw fence.
- Not verified in the pane: a real clipboard write, ⌘⏎ — both since confirmed by KS in Safari.
  Firefox remains unverified.

## Now / Next

Now: draft PR. Next: land.

## Deferred

- Glossary popovers (`spec.glossary`) — approved in principle, moved to a later milestone (KS, 2026-09-23).

## Rejected

- Option ↔ diagram highlight (`options[].highlight`): depends on Mermaid's internal SVG ids, fragile
  across versions (KS, 2026-09-23).

## Resolved open questions (#64, KS 2026-09-23)

- Mermaid stays pinned at 11.6.0; an upgrade is a separate change with its own browser pass.
- Mermaid stays vendored (~2.7 MB) for offline, deterministic rendering.
- Layout and theme belong to the reader, not the spec (v2.1): the agent's `layout` is only a hint.

## Confirmed contracts

- Answer shapes, exit codes, stdout document and token scope are unchanged from v1; `spec_version` stays 1.
- CSP `script-src 'self'`; vendored `mermaid@11.6.0` (MIT, sha256 `e9dd0d9a…`, license banner
  prepended) and `highlight.js@11.11.1` (BSD-3, sha256 `c4a399dd…`), both loaded lazily.
- Mermaid runs with `securityLevel: "strict"`; its SVG is inserted without a second sanitizer.
- The record's raw JSON tail uses a fence longer than any backtick run in the spec.
