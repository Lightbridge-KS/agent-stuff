---
summary: Progress tracker for the `harness-design` Concept skill v1 (plugins/coding) —
  distilled from three mid-2026 talks (Qu/Vercel, Schmid/DeepMind, Chase/LangChain); the
  two-axis decision rule for how much harness to own, the Decide / Build / Learn procedure,
  no house defaults and no tool budget by KS decision; landing via PR.
read_when:
  - resuming or revising the harness-design skill
  - re-verifying references/landscape.md after a vendor release
  - deciding whether the deferred items (house defaults, tool budget, Harbor runner) come back
---

# harness-design v1 — progress

Design (the decision): settled in session 2026-09-16 (KS), plan file
`~/.claude/plans/purrfect-swimming-phoenix.md`; no separate design doc — the skill body *is*
the design; sources and lessons in `plugins/coding/skills/harness-design/references/sources.md`.
Learning workspace with transcripts, slide sheets and per-talk notes:
`~/Movies/YouTube/Agent-Building/`.

Origin: KS watched three talks on agent harnesses and asked what skill would carry the
distillation. Gap identified: `agentic-architecture` *maps* an agent's organs neutrally and
`extensibility-architecture` maps how a system changes after it ships; none decides **how
much harness to write**, then what to build and how to make it improve.

## Decisions (resolved 💡, form record in `~/.lightbridge/projects/-Users-kittipos-Movies-YouTube-Agent-Building/asks/`)

- **Kind / home:** Concept skill (+ dated Reference appendix), Authored,
  `plugins/coding/skills/harness-design/`. A separate skill, not a section of
  `agentic-architecture`: that lens stays neutral, this one takes a stance.
- **House defaults:** *none in the skill.* The Radiology AI Unit constraints (no hosted harness
  near PHI or `10.6.x.x`, sandbox local or Beelink, allow-list starts empty, clinical paths as
  a control slice) stay in CLAUDE.md and the hospital-network doc. Diverged from the agent's
  recommendation to encode inline.
- **Tool budget:** *none.* Step 6 keeps the per-tool justification with no number attached.
  Diverged from the recommendation of a threshold smell.
- **Evals:** adopt the Harbor task *shape* (environment + instruction + verifier); defer the runner.
- **Cross-link:** one sentence in `agentic-architecture/SKILL.md` pointing here.

## Milestones

- [x] Learn the sources: transcripts + slide contact sheets for all three talks; per-talk notes; cross-talk `NOTES.md` — 2026-09-16
- [x] Draft `SKILL.md` (233 lines), `references/landscape.md` (`verified: 2026-07/08`), `references/sources.md`; `uv run bin/validate.py` green (44 skills) — 2026-09-16
- [x] Cross-link from `agentic-architecture` (one sentence in the core reframe) — 2026-09-16
- [x] Install (`bin/install.py --claude --codex coding/harness-design`); both registry symlinks resolve — 2026-09-16
- [x] Route-test (`claude -p`, sonnet, no MCP): the LangGraph-pipeline prompt loaded `harness-design` and ran distribution check → control check → evals first; the organ-mapping prompt loaded `agentic-architecture` — **PASS** — 2026-09-16
- [ ] Land on `main` via PR (branch `feat/harness-design-skill`)
- [ ] First real use on a project; revise

## Now / Next

Now: PR. Next: first real use.

## Deferred

- House defaults for the Radiology AI Unit as a private companion skill in
  `agent-stuff-private`, if real use shows the reader keeps re-deriving them.
- A custom-tool threshold smell, if real use shows tool sprawl the justification cell does not catch.
- Harbor as the house eval *runner*; a `skill-creator` eval suite for this skill once content stabilises.
- Re-verify `references/landscape.md` at the next Eve, Deep Agents or Interactions API release.

## Confirmed contracts

- The skill's output is one design section (template in SKILL.md §6) embeddable in an
  `agentic-architecture` doc, or standalone `docs/design/harness-design.md`.
- Vendor versions are never asserted from the skill; `landscape.md` carries a `verified:` date
  and an instruction to fetch current docs.
