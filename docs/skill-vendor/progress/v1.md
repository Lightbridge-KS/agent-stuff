---
summary: Progress tracker for skill-vendor engine v1 — manifest + sync/doctor/attest/list,
  crabbox (first-party detached) and gog (co-shipped) as the first live entries.
  Milestones with commit SHAs.
read_when:
  - resuming or continuing the skill-vendor engine build
  - checking what v1 shipped vs deferred before changing scripts/skill-vendor
  - registering the next vendored skill entry
---

# skill-vendor engine v1 — progress

Design (the decision): [`../design.md`](../design.md) — approved 2026-07-25 (KS).
One invariant (served skill bytes are known-good for the installed binary), three
knowledge sources: co-shipped / first-party tag / third-party attestation. Manifest
`~/.lightbridge/skill-vendors.toml` hand-edited in v1; `attest` is the only machine
writer; co-shipped entries included so doctor covers the whole Vendored category.

Origin: AGENTS.qmd *Skill vendors* terminology (agent-instruction, 2026-07-25) named
the detached-vendored pin obligation; this engine is its mechanism. First driver:
the `crabbox` skill — CLAUDE.md already points agents at it, but no registry serves
it yet.

## Milestones

- [x] Design doc + this tracker committed — `ea3f64c`
- [x] `scripts/skill-vendor` skeleton — manifest load/validate (mode by key shape,
  `tag` xor `ref`), version resolution (`--version`, first-semver parse), `list` — `854aef0`
- [x] `doctor` — per-entry checks + registry-wide invariant scan; exit 0/1/2 — `854aef0`
- [x] `sync` — worktrees under `~/.lightbridge/skill-vendors/worktrees/`, registry
  relink; failure honesty (no tag / no skill dir → nonzero, symlinks untouched) — `854aef0`
- [x] `attest` — stamp `verified-against` + `verified-on` by line surgery — `854aef0`
- [x] PATH link (like `lightbridge`) + agent-stuff validation gates green — `854aef0`
  (17 behavioral tests; validator + full `just test` suite green)
- [x] **crabbox live** (first-party detached): worktree at `v0.40.0` =
  `crabbox --version`, symlinks in `~/.claude/skills/` + `~/.codex/skills/`, entry
  clean — 2026-07-25
- [x] **gog live** (co-shipped): entry added; pre-existing hand-made symlinks already
  matched the engine target, adoption was a no-op — 2026-07-25
- [x] Skew drill (live): shim `0.39.0` → doctor exit 1 naming
  `skill-vendor sync crabbox`; shim `0.41.0` (unpublished tag) → exit 2, honest
  refusal; restore → exit 0 — 2026-07-25

## Now / Next

- **Now:** v1 shipped. `doctor` exits 1 on the five known registry real-dirs — that
  is the drain worklist (my_config backlog: migrate in-place skills, then the
  Adopted shelf).
- **Next:** first third-party attested entry, when one graduates from the
  `~/.agents/skills/` drain.

## Deferred

- snapshot/url mode (no-repo skills; `/.well-known/agent-skills/` + sha256)
- `add` scaffolder; SessionStart doctor hook; `lb doctor` composition
- third-party attested entry — no real candidate yet; first one likely graduates
  from the `~/.agents/skills/` drain (agent-instruction backlog)

## Open questions

- Semver parse fallback: some CLIs print bare `0.40.0`, some `tool 0.40.0 (sha)`,
  some need `version` subcommand — is "first semver in output" + `version-cmd`
  override enough? Decide when a real counterexample appears.
- Should `list` warn (not fail) on registry symlinks it doesn't manage that point
  into install trees — i.e. hand-made vendored links predating the engine?

## Confirmed contracts

- Doctor exit codes: 0 clean / 1 skew / 2 broken — external seam for `lb doctor`
  and hooks; do not repurpose.
- Registry entries managed by the engine always resolve through
  `~/.lightbridge/skill-vendors/worktrees/<name>/` (detached) or the install tree
  (co-shipped) — never a repo's live `main` checkout.
